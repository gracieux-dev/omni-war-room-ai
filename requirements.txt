# OmniWarRoom AI — pinned dependencies
# Python 3.12 required

# UI
streamlit==1.57.0

# LangChain / LangGraph stack (must stay in sync)
langchain==1.3.2
langchain-core==1.4.0
langchain-groq==1.1.2
langchain-google-genai==4.2.3
langchain-aimlapi==0.1.0
langchain-mcp-adapters==0.2.2
langgraph==1.2.2
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.3.15

# LLM clients
groq==0.37.1
google-genai==1.75.0

# MCP
mcp==1.27.1

# Data / async
pydantic==2.13.4
pandas==3.0.3
nest-asyncio==1.6.0
httpx==0.28.1

# Utilities
python-dotenv==1.2.2
rich==14.3.4
plotly==6.7.0

# Memory
cognee==1.1.0

# Voice — transcription via httpx direct (no SDK needed in ui/app.py)
# speechmatics-python removed: app.py uses httpx directly, not the SDK

# pyttsx3 removed: local TTS only, requires espeak system libs not on cloud
# fpdf2 removed: only used by lol.py (pitch deck generator), not the web app
